
import { Spacer } from '../styles/containers.style'
import { ExtraSmall, MediumText, SmallText } from '../styles/text.style'

export const Info = (): JSX.Element => {
  return (
    <Spacer>
      <ExtraSmall> If you stumbled across that application you can use ETH (Ethereum) against BTC (bitcoin) to check the data display functionality </ExtraSmall>
    
    </Spacer>
  )
}

